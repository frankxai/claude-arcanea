---
description: "Arcanea Orchestrator — alias for /ao. Usage: /arcanea-orchestrator [status|promote|digest|coach|cleanup|plan|handover]"
thinking: true
---

# Arcanea Orchestrator

This is an alias for `/ao`. Subcommand: `$ARGUMENTS`

Read `.claude/commands/ao.md` instructions and execute with the same `$ARGUMENTS`.
